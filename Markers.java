/*Make a class of markers and include the type of marker(String)
 * color of the marker(String)
 * and number of markers(int)
 * Then create the constructors and getters/setters
 */
public class Markers {

	
	public String toString() {
		return String.format("The type of marker is " + markerType + ".\n The color of the marker is " + markerColor + ".\n"
				+ "The number of markers is " + numOfMarkers + ".");
	}
}

